[project]
name = "spm-reporting"
version = "0.1.0"
description = "Reproducible Python reporting for SPM discretization comparison results."
requires-python = ">=3.10"
dependencies = [
  "matplotlib>=3.8",
  "numpy>=1.26",
  "pandas>=2.2",
]

[tool.uv]
package = false
